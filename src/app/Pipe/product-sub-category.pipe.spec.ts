import { ProductSubCategoryPipe } from './product-sub-category.pipe';

describe('ProductSubCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductSubCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
